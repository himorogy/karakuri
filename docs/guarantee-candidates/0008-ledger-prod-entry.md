# 0008-ledger-prod-entry

### `images/runtime-base/shims/dotenvx`

- テスト未作成 prod 鍵が注入され、実行を伴わず、厳格モードも伴わない呼び出しでは警告が出ない（`§8` の警告条件のうち実行の有無の軸に否定対照が無く、実装から当該条件を落としても現行のテストは全て緑のまま）

### `images/runtime-base/bin/karakuri-context`

- テスト未作成 解決結果の記録が無いとき、その不在に由来する行が1行も出ない（現行の検査は特定の語を含む行の不在しか見ないため、別の文言で「見つからない」と言い出しても検出できない）
- テスト未作成 対話シェルの起動あたり、認証確認コマンドの呼び出しがちょうど1回であること（現行の検査は呼ばれた痕跡の有無だけを見るため、複数回呼んでも検出できない）

### `images/runtime-base/bin/secrets-ingest.sh`

- テスト未作成 取込失敗のメッセージが該当行の行番号を含むこと（現行の検査は入力を反射しないことだけを見ており、位置情報を出す側は約束として立っていない）

### `images/runtime-base/bin/prod-entrypoint.sh`

- テスト未作成 起動経路が secret の取込器を実際に起動していること（`§6` が宣言する入力検査の同一性は委譲の配線に依存するが、現行の検査は列挙された4ケースの結果しか見ないため、独自パーサに置き換わっても検出できない）
