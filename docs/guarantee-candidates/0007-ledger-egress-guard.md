# 0007-ledger-egress-guard

### `packages/egress-guard/tests/firewall-config.test.sh`

- テスト未作成 同梱テンプレートがちょうど3種であること（台帳 `§4` は個数を名指しするが、現在の検査は `templates/*.json` を走査して各件が検証を通ること・1件以上あったことだけを固定しており、テンプレートが増減しても台帳側が黙って古くなる）
