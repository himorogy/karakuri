# セキュリティポリシー

このリポジトリは、開発コンテナの通信制限（`@himorogy/egress-guard`）と、平文の秘密がリポジトリへ入るのを止める検査（`@himorogy/env-guard`）を提供します。いずれもセキュリティ境界そのものを構成するため、脆弱性の報告を歓迎します。

## 報告方法

**公開の Issue で報告しないでください。**

このリポジトリの **Security** タブから「Report a vulnerability」を選び、非公開の脆弱性報告を作成してください。この経路は GitHub が提供するもので、修正が公開されるまで内容は報告者とメンテナのみが閲覧できます。

報告に含めていただけると助かる情報:

* 影響を受けるパッケージとバージョン
* 再現手順、または攻撃が成立する条件
* 想定される影響（何が読めるか、何が実行できるか、どの境界が破られるか）
* 動作を確認した環境（OS、コンテナランタイム、Node.js のバージョンなど）

## 対応の流れ

1. **受領の連絡** — 3 営業日以内を目安に返答します
2. **影響の確認** — 再現と影響範囲の特定
3. **修正と公開** — 修正版をリリースし、報告者の希望に応じてクレジットを記載します

修正の公開まで、報告内容を第三者へ開示しないようお願いします。

## 対象範囲

### 脆弱性として扱うもの

* 通信制限を回避して、許可されていない宛先へデータを送出できる経路
* 検査をすり抜けて、平文の秘密をコミットできる経路（検査が「問題なし」と報告してしまう入力）
* 暗号化された秘密を、想定されていない手段で復元できる経路
* スクリプトが root 権限で動作することを利用した、権限昇格や任意コード実行
* 設定ファイルの解析における、意図しない挙動を誘発する入力

### 既知の制限として文書化されているもの

以下は設計上受け入れている制約であり、脆弱性としては扱いません。報告の前にご確認ください。

* [`packages/egress-guard/docs/design.md`](./packages/egress-guard/docs/design.md) §3 — 受容した残余リスク
* [`packages/egress-guard/docs/spec.md`](./packages/egress-guard/docs/spec.md) §9 — 決定済みの制限
* [`packages/egress-guard/docs/known-issues.md`](./packages/egress-guard/docs/known-issues.md) — 未実装・未検証・保留の課題

これらに該当する内容であっても、**想定より影響が大きいことを示せる場合**は報告してください。受容の判断そのものが誤っている可能性は常にあります。

### 対象外

* 依存パッケージの脆弱性そのもの（上流へ報告してください。ただし、このリポジトリでの使い方が影響を増幅している場合は対象です）
* 設定を意図的に緩めた状態でのみ成立する問題
* このリポジトリが管理していないインフラ（npm レジストリ、GitHub の実行基盤）

## サポート対象

各パッケージの最新のマイナーバージョンに対して修正を提供します。

## 公開経路について

パッケージが npm へ届くまでの経路と、その設計上の判断は [`docs/secure-publish.md`](./docs/secure-publish.md) に記録しています。

公開経路は現在、出所証明（provenance）を伴う形へ移行中です。移行完了後は、利用者側で以下により出所を検証できます。

```sh
npm audit signatures
```

移行の進捗は [`docs/secure-publish.md`](./docs/secure-publish.md) §8 に記録しています。
